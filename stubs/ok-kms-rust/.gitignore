# Real ok-kms-rust SDK artifacts installed by `just kms-crate` (never committed — C3).
# `just kms-crate-restore` removes them via `git clean -fdx stubs/ok-kms-rust`.
# NOTE (round-4 / Spec §5.2): do NOT add any early-exit "already installed" marker file here —
# a marker on an ignore path survives `git clean` without `-x` and would gate the next install.
/libs/
