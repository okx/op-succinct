#!/usr/bin/env bash
# Stub: docker
STUB_LOG="${STUB_LOG_DIR}/docker.log"
echo "docker $*" >> "${STUB_LOG}"
if [[ "$1" == "import" ]]; then
    if [ "${DOCKER_STUB_FAIL:-false}" = "true" ]; then
        echo "Cannot connect to the Docker daemon" >&2
        exit 1
    fi
fi
